import React, { FC } from 'react';
import StackFactory from '../components/stack-factory';

const AuthStack: FC = () => {
  return <StackFactory stackName={'AuthStack'} />;
};

export default AuthStack;
